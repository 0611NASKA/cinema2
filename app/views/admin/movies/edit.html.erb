<% @page_title = "作品の編集" %>

<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "作品詳細に戻る", [:admin, @movie] %></div>

<%= form_for [:admin, @movie] do |form| %>
  <%= render "edit_movie_form", form: form %>
  <div><%= form.submit %></div>
<% end %>
